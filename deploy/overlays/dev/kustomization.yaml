apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base

commonLabels:
  environment: dev

patchesStrategicMerge:
  - deployment-patch.yaml

replicas:
  - name: airports-ai
    count: 1

images:
  - name: airports-ai
    newTag: latest
