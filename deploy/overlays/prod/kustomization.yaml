apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base

commonLabels:
  environment: prod

replicas:
  - name: airports-ai
    count: 3

images:
  - name: airports-ai
    newTag: stable
