apiVersion: v1
kind: Service
metadata:
  name: airports-ai
  labels:
    app: airports-ai
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: airports-ai
